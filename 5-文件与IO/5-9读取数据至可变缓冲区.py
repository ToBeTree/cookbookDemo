# 你想直接读取二进制数据到一个可变缓冲区中，而不需要做任何的中间复制操作。 或者你想原地修改数据并将它写回到一个文件中去
"""
readinto用来为预分配内存的数组填充数据
文件对象的readinto填充已存在的缓冲区
bytearray分配内存空间
"""
import os.path
from itertools import chain
bytearray()
a = [{'a': 1, 'b': 3}]
print(a[0]['a'])
